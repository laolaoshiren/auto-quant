# 安全策略

本项目是一套**用真实资金在币安 USDⓈ-M 合约上下单**的自动化交易系统。
它同时持有一份**长期有效的交易所交易凭据**。因此本项目的安全问题通常不是
"页面被篡改"，而是"账户被接管"。

---

## 报告安全漏洞

**请不要开公开 issue。** 公开描述一个凭据泄漏路径或下单绕过路径，等于在修复之前
把利用方法交给所有人。

请改用以下**私密**渠道：

1. **GitHub Security Advisories**（首选）
   仓库页面 → `Security` → `Report a vulnerability`。
   这条通道是私密的，便于后续协调披露与 CVE。
2. **邮件**：`<待填写>`（项目所有者请填入一个真实可用的安全联系邮箱）

请在报告中尽量包含：影响范围、复现步骤、以及**你已经脱敏**的证据。

我们会在确认收到后尽快回复，并在修复发布后（经你同意）致谢。

---

## 支持的版本

项目处于 **1.0 之前**，尚未发布任何带版本号的正式版本。

| 版本 | 支持状态 |
| --- | --- |
| `main` 分支 | ✅ 唯一受支持的版本 |
| 任何历史提交、分支或 fork | ❌ 不支持 |

安全修复只合并到 `main`。请始终基于最新的 `main` 运行。

---

## 这个项目特别敏感的几点

以下每一条都是**本应用独有的**风险面，而不是通用清单的复制。

### 1. 开启「提现权限」的 API Key 是最高风险

本应用只需要**合约交易权限**，**不需要提现权限**。

- 若泄漏的 Key 同时具备提现权限，攻击者可以直接转走账户全部余额。
- 若 Key 没有提现权限，最坏情况是攻击者乱下单，损失仍被账户余额限制。

因此请务必：**关闭提现权限**、**绑定 IP 白名单**。

> 控制台的启动预检会读取 `/fapi/v2/account` 的 `canWithdraw` 标志并以
> **警告**（而非错误）提示。这是刻意的：`canWithdraw` 只是权限**标志位**，
> 对于**子账户**密钥，实际提现仍由主账户控制，标红会变成误报并训练操作员
> 忽略整个面板。请按提示自行判断。

### 2. `data/` 目录 = 账户

`data/` 目录同时包含：

- `data/.master.key` —— 加密交易所凭据用的 **AES-256-GCM 主密钥**；
- `data/autoquant.sqlite` —— 其中 `exchange_accounts.api_secret_enc` 是**加密后的**
  API Secret；`ai_models` 中的 LLM Key 同样加密存储。

**这两样合在一起等价于明文凭据。** 只泄漏数据库（有密文无主密钥）还好；
只泄漏主密钥（无密文）也好；**一起泄漏就是账户泄漏**。

`data/` 已在 `.gitignore` 中排除，请保持这样，并且不要把它放进任何备份、
截图、issue 附件或云同步目录。

### 3. 控制台可以下真实订单

控制台**登录之后就能下真实订单**。仅有一个密码不足以把它暴露到公网。

若要公网访问，请在反向代理层再叠加至少一层：`basic_auth`、Cloudflare Access、
或 IP / 国家限制。详见 `README.md` 的「部署到服务器」一节。

### 4. 第三方 LLM 端点会收到完整交易上下文

每次决策都会把**完整提示词**发给所配置的 LLM 提供商，其中包含：

- 账户权益与余额；
- 当前持仓、方向、杠杆、入场价与保护价位；
- 近期成交记录与盈亏；
- 候选标的的市场数据。

**选择 LLM 提供商因此是一个数据共享决策，而不只是成本/效果决策。** 请确认你接受
该提供商的隐私与保留策略，并注意：本项目**永不记录 API Key**，但提示词本身会被
持久化在决策审计里（这是可审计性的要求）。

### 5. 生产环境必须显式设置 `MASTER_KEY` 与 `JWT_SECRET`

首次启动时，若未提供 `MASTER_KEY`，系统会**自动生成**一把并写入
`data/.master.key`；`JWT_SECRET` 同理（`data/.jwt.secret`）。

这是**开发便利**，不是生产配置：

- 自动生成的密钥会随容器重建 / 目录清理而**丢失**，届时所有已保存的交易所凭据
  都将**无法解密**；
- 密钥落在磁盘上，扩大了泄漏面。

生产环境请通过环境变量显式注入这两项（参见 `.env.example`）。

---

## 安全设计

本项目的安全机制（简要）：

- **凭据静态加密**：交易所 Secret 与 LLM Key 使用 **AES-256-GCM** 加密存储，
  自描述格式 `v1:<iv>:<authTag>:<ciphertext>`。**解密后的内容永不返回给前端。**
- **口令哈希**：操作员口令使用 **scrypt** 派生（`scrypt$N$r$p$salt$hash`），
  校验使用**恒定时间比较**。
- **JWT 会话**：请求签名校验同样使用恒定时间比较。
- **风控是不可绕过的硬闸门**：模型输出被当作**不可信输入**。每一张订单都必须经过
  `packages/server/src/risk/engine.ts`，那里的限制在代码里强制执行，
  **模型无法通过提示词覆盖**（杠杆钳制、名义价值上限、保证金预算、强制止损止盈、
  回撤守卫、熔断、节流）。每一次钳制都会写进决策审计。
- **实盘冒烟测试有硬闸门**：`packages/server/src/scripts/liveSmokeTest.ts`
  必须显式传入 `--confirm` 才会下单，名义价值硬上限 **20 USDT**，
  清理步骤（撤单 + 平仓）写在 `finally` 块里 —— 中途异常也会执行，
  结束时回读交易所确认账户干净。
- **急停开关**：`GLOBAL_TRADING_DISABLED=true` 拒绝启动任何机器人；
  `DRY_RUN=true` 读真实行情但不真正下单。

---

## 不在范围内

- 上游依赖（币安 API、各 LLM 提供商、npm 包）自身的漏洞 —— 请报告给对应上游，
  但欢迎同时告知我们以便评估影响。
- 因自己配置不当造成的暴露，例如把控制台无认证地暴露到公网、
  或使用了带提现权限的 Key。

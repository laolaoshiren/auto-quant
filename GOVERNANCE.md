# 治理

本文档说明本项目**如何做决定**、**角色如何划分**、以及**一个人如何从使用者变成维护者**。

它的存在是为了让协作可预期：一个贡献者应该能事先知道自己的改动会经过什么流程，
而不是靠猜维护者的心情。

---

## 决策原则

当不同目标冲突时，优先级如下（**自上而下，不可倒置**）：

1. **不造成资金损失** —— 任何改动如果可能让仓位失去保护、让风控被绕过、
   或让账目无法与交易所对上，无论它带来多少便利，都不合入
2. **正确性** —— 账目口径、交易所接口契约、数据迁移不能"差不多就行"
3. **可验证性** —— 无法被验证的改动等于不存在。宁可不加，也不要加一个说不清是否生效的功能
4. **可读性** —— 代码是写给下一个人的。这个项目刻意保留了大量解释「为什么」的注释
5. **功能与性能** —— 排在最后。一个功能再多，只要前四条站不住，就不进主干

这条顺序不是口号。它解释了为什么下面这些看起来"低效"的做法是刻意的：

- 每次开仓都要挂保护单并**回读交易所确认**，而不是相信下单成功
- 对账要跑三遍（运行期记账、持仓对账、成交历史重建），而不是只记一次
- 依赖许可证检查遇到 copyleft 直接**失败**而不是警告
- 策略体检要区分"模型没回答"和"风控拒绝了"，而不是统一报"失败"

---

## 角色

| 角色 | 能做什么 | 如何获得 |
| --- | --- | --- |
| **使用者** | 提 issue、参与 Discussions、提交 PR | 无门槛 |
| **贡献者** | 已合并过至少一个 PR | 自动 |
| **评审者** | 被邀请评审他人 PR，其 review 有分量但不阻塞合并 | 由维护者邀请 |
| **维护者** | 合并 PR、发布版本、管理 issue 与标签、决定路线图 | 见下 |

### 成为维护者

没有固定的贡献次数门槛 —— 那会鼓励凑数。判断标准是**是否表现出对这个项目风险的理解**：

- 提交的改动有测试，且注释解释的是「为什么」而不是「做了什么」
- 涉及下单路径的改动，**主动说明验证到了哪一级**，而不是声称"应该没问题"
- 评审时能指出别人改动里的风险，而不只是风格问题
- 对不确定的事情**说"我不确定"**，而不是用听起来合理的猜测填补空白

最后一条尤其重要。这个项目面向的是一个会真实亏钱的场景，
一个敢于说"这一点我没能验证"的人，比一个总是显得很确定的人更值得信任。

维护者由现有维护者邀请产生。**如果项目长期只有一位维护者，这一节就是等待被行使的。**

---

## 决策流程

### 日常变更：惰性共识

绝大多数改动走正常 PR 流程：

1. 开 PR，填模板
2. CI 通过
3. 至少一位维护者或评审者 approve
4. squash 合并

**没有反对意见即视为通过。** 不需要为每个改动开会讨论。

### 需要 RFC 的变更

以下改动**必须**先开一个 RFC issue 讨论，不能直接提 PR：

| 类型 | 为什么 |
| --- | --- |
| **破坏性变更** | 会直接影响正在运行的实例，需要迁移路径与提前通知 |
| **风控模型变更** | 改变哪些提案会被拒绝或钳制，影响所有策略的行为 |
| **新增交易所** | 会引入新的接口契约、精度规则、错误语义，不是适配层能独自消化的 |
| **数据模型变更** | 数据库迁移无法回退，必须提前确认 |
| **许可证变更** | 见下文，涉及所有历史贡献者 |
| **引入新依赖类别** | 例如引入原生依赖，会改变部署要求 |

RFC 的形态不要求正式：一个 issue，写清楚**动机、方案、影响范围、备选方案**即可。
在 RFC 上达成共识后再写代码，比写完再争论省时间。

### 紧急修复

安全漏洞或正在造成资金损失的问题，**可以直接提 PR 并合并**，事后补 RFC。
这条例外只在真的有紧急情况时使用 —— 它不是一个"我觉得很急"的通道。

---

## 许可证与贡献者权利

**提交 PR 即表示同意你贡献的代码按项目当前许可证授权。** 详见
[CONTRIBUTING.md](CONTRIBUTING.md#贡献授权)。

这条例款的存在是为了让下面这件事成为可能：

一旦代码被合并，那部分代码的版权**同时属于贡献者**。
如果项目将来需要变更许可证（例如从专有转为开源，或在宽松与 copyleft 之间切换），
**必须取得每一位贡献者的同意**才能变更整体授权 —— 否则只能追溯剔除那些代码。

事先写明，可以让将来的一次性决定可行，而不是翻出一份贡献者名单逐个联系。

---

## 变更许可证的流程

如果真的需要变更（例如决定开源）：

1. 开一个 RFC issue，说明变更理由与目标协议
2. **列出所有贡献者**（`git log --format='%aN' | sort -u`），逐一取得书面同意
3. 更新 `LICENSE`、`package.json` 的 `license` 字段、README 的许可章节
4. 检查依赖许可证是否与新协议兼容（`node scripts/generate-notices.mjs`）
5. 参考 [docs/LEGAL.md](docs/LEGAL.md) —— **在变更之前咨询执业律师**

---

## 行为准则

不设独立的 `CODE_OF_CONDUCT.md`。理由：这类文件常常被写成一长串无人阅读的条文，
而这个项目真正需要的只有几条清楚的规则：

- **对事不对人。** 评审意见针对代码，不针对作者
- **不装懂。** 不确定就说不确定 —— 在这个项目里，一个自信的错误判断会让人亏钱
- **认真对待别人的时间。** 提 issue 前先搜索、先读文档；提 PR 前先跑通 CI
- **安全问题上不谈条件。** 涉及凭据、密钥、绕过风控的讨论没有"通融一下"的余地

如果出现骚扰、歧视或人身攻击，维护者会直接移除相关人员。
本项目是私有的，但这条规则在开源后同样适用。

---

## 维护者离开或失联

如果维护者长期不活跃（连续三个月无响应）：

- 评审者可以自行组织合并简单改动
- 涉及风控与下单路径的改动**暂缓**，直到有具备相应判断力的人接手
- 不鼓励 fork 出走 —— 但如果真的发生，这是许可证允许的权利，不应被视为背叛

---

## 本文档的修订

治理规则本身也按 RFC 流程变更：开 issue、讨论、达成共识后修改本文档。

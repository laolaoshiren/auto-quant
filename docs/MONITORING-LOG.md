# 运行监控记录 · 机器人 #5「测试2」

**监控目标**：控制台上的机器人详情页（地址见本地部署配置，不写进仓库），至少 5 小时。
**体检脚本**：`tools/healthSnapshot.mjs`（只读，一条命令输出全部指标）。

每轮记录：发现 → 结论 → 动作。

---

## 轮次 1 · 2026-09-16 20:50（第 0 分钟）

### 基线

| 指标 | 值 |
| --- | --- |
| 机器人状态 | `running` |
| 最近 20 轮 | **20 成功 / 0 失败** |
| 累计成交 | 15 笔 |
| 毛 / 手续费 / 净 | -0.0294 / -0.2894 / **-0.3189** |
| 今日已实现 | -0.5910（权益的 **-6.11%**） |
| 本地持仓 / 活动委托 | 0 / **24（全部陈旧）** |
| 提示词规模 | 58,266 tokens |
| 延迟 | 平均 **48,507ms**（最小 29,098 / 最大 91,665） |
| 成本 | **2,695,267 tokens/小时**（40 轮） |

---

### 发现 1 · 延迟涨了 3.8 倍，提示词涨了 2.6 倍 ⚠️

| | 早先（周期 #1–40） | 现在（#40–54） |
| --- | --- | --- |
| 平均延迟 | 12.6 秒 | **48.5 秒** |
| 提示词 | 22,099（3 候选） | **58,266（11 候选）** |

**48.5s ÷ 180s 周期 = 每轮 27% 的时间在等模型；最慢一轮 91.7 秒 = 51%。**

**原因**：策略配置 `coinPoolLimit: 25`，而提示词预算只允许 11 个候选
（每个标的约 5,159 tokens，预算 60,000）。预算被打满。

**这不是 bug** —— 预算是主动限制，策略配置与预算不一致是调参问题。
**但它有实际后果**：延迟吃掉周期、token 消耗 2.7M/小时。

**动作**：记录。**调整预算或候选数是产品决策，需要用户定** ——
预算调小 → 更省更快但候选更少；候选调少 → 同上。

---

### 发现 2 · 熔断警告每个周期重复两条 ⚠️ **（BUG，已定位）**

一小时 920 条日志，其中：

```
× 21  单日亏损熔断：今日已实现亏损 $0.59，占权益 6.03%（上限 5%）。
× 21  [测试2] 单日亏损熔断：今日已实现亏损 $0.59，占权益 6.03%（上限 5%）。
```

**根因（读代码确认）**：`autoTrader.emit()` 的一次调用产生**两条库记录 + 两个前端事件**，
因为同一个消息走了两条落盘路径：

```ts
// autoTrader.ts:307
private emit(level, message): void {
  log[level](`[${trader.name}] ${message}`);           // ① → 触发全局 sink
  runtimeLogs.write(trader.id, level, 'trader', message);  // ② → 直接写库
  eventBus.publish({ type: 'log', ... });              // ③ → 直接推前端
}

// server.ts:1616 —— 全局 sink（①②两条路径最终都到这里）
setLogSink((level, scope, message) => {
  runtimeLogs.write(null, level, scope, message);      // trader_id = NULL
  eventBus.publish({ type: 'log', traderId: null, message: `[${scope}] ${message}` });
});
```

于是**同一个事件**变成：
- 一条 `trader_id = NULL`、带 `[测试2]` 前缀（服务器日志视图）
- 一条 `trader_id = 5`、不带前缀（机器人日志视图）
- 两个 WebSocket `log` 事件 → 界面上同一件事出现两次

**影响**：约 42/920 条，是**日志噪音**而非资金问题。但它确实让同一件事在界面上重复，
而且 `log[level]` 走 sink 的那条**丢了机器人归属**（`trader_id = NULL`），
所以按机器人筛选日志时会漏看它。

**不修的原因**：`autoTrader.ts` 正被另一个代理编辑（陈旧委托修复）。
**等它完成后处理**，避免制造冲突。

### 发现 2b · 状态型警告每周期都写

`单日亏损熔断` 和 `候选池裁剪` 都是**每个周期必然发生**的状态，
却按事件每条写一次（21 次/小时，会持续到当天结束）。

**一个每个周期都触发的警告，按事件写等于没有警告** —— 它快速填满日志保留窗口，
把真正的异常埋掉。

**动作**：与发现 2 一起处理 —— 改为**状态变化时写一次**，或按小时聚合。

---

### 发现 3 · 成交量偏高，手续费仍是主要亏损来源 ⚠️

```
2026-09-15:  4 笔   净 +0.2721   手续费 0.0705
2026-09-16: 11 笔   净 -0.5910   手续费 0.2189
```

手续费占毛亏损的 **37%**，且成交笔数一天内从 4 涨到 11。

**熔断（-6.11% > 5%）是保护机制，不是故障** —— 它已经阻止了当天所有新开仓
（周期 #38 的两笔 `open_short` 都被它拦下）。

**动作**：记录。**根因方案已写好待批**：`docs/PROPOSAL-model-performance-context.md`。

---

### 发现 4 · 24 条陈旧委托 / 3 组重复成交

两项都已知，修复已在途中或已完成：

| 问题 | 状态 |
| --- | --- |
| 24 条委托标着 `NEW` 但交易所挂单为 **0** | 代理修复中 |
| 3 组重复记账（LSKUSDT ×2、SYNUSDT ×2、SYNUSDT ×2） | **代码已修**；**生产库那 3 行仍待人工决定是否删除** |

---

### 发现 5 · 决策流吞掉 `skipped` 状态 ⚠️ **（BUG）**

周期 #38 里两笔 `open_short` 被熔断拦下（`status: 'skipped'`），
但决策流把它们渲染成正常的「开空」，看不出被拦。

`ExecutionLogEntry.status` 有 **4** 种取值，界面只过滤了 **2** 种。
服务端在 **6 处**产生 `skipped`（熔断、节流、冷却…）。

**动作**：代理修复中。

---

### 发现 6 · 出现过一次空补全

```
× 1  empty completion with finish_reason=length for custom
```

`finish_reason=length` 说明**输出被 max_tokens 截断**了。只出现 1 次。

**动作**：记录并观察。若复现，说明 `maxTokens` 对当前提示词规模偏小
（现在每轮输出 5,898–16,384 tokens，波动很大）。

---

## 轮次 2 · 2026-09-16 21:00（第 10 分钟）

### 状态：稳定

| 指标 | 轮次 1 | 轮次 2 |
| --- | --- | --- |
| 状态 | running | running |
| 最近 20 轮 | 20 成功 | **20 成功** |
| 最近周期延迟 | 29,098ms | **27,081ms** |
| 成交 / 持仓 | 15 / 0 | **15 / 0**（无变化） |
| 今日已实现 | -6.11% | -6.11% |

**延迟不是单调上升**：27,081ms 是最近最快的一轮，而 30 轮平均 48.5 秒。
它在 **27–91 秒之间大幅波动**，不是持续恶化。这一点修正了轮次 1 的表述。

---

### 发现 7 · 熔断生效时仍在花钱调用模型 ⚠️ **（浪费，已定位，待修）**

代码顺序（`autoTrader.ts`）：

```
L651  const breaker = checkCircuitBreakers(...)    ← 检查熔断
L655  if (breaker.blocked) this.emit('warn', ...)   ← 只记日志
L713  buildSystemPrompt(...)                        ← 构建 58k token 提示词
L725  await this.deps.model.complete(...)           ← ★ 花钱调用模型
L796  if (breaker.blocked) { ... }                  ← ★ 到这里才真正拦截
```

**熔断在 L651 检查，却在 L796 才执行——中间隔着一次完整的模型调用。**

每个周期都在做：构建 5.8 万 token 提示词 → 付费请求模型 → **把全部决策丢掉**。

实测损失：熔断生效 + 持仓 0 → 每轮 58,266 输入 + 约 9,000 输出
→ **约 134 万 tokens/小时，产出为零**。熔断按"单日"计算，可能持续数小时。

**修法（不是简单地"熔断就跳过模型"）**：

熔断**只拦新开仓，不拦平仓**。有持仓时模型必须继续跑（决策里可能有平仓）。
**只有「熔断生效 且 无任何持仓可管理」时跳过模型调用才安全**——此时模型
不可能产出任何可执行的东西。

跳过时要**照常写一条决策记录**说明原因，否则操作者会以为机器人卡住了。

**待修原因**：`autoTrader.ts` 正被另一个代理编辑。等它完成立即处理。

---

### 待清理的临时文件

验证时留下的探针，代理完成后删除：
`packages/web/src/components/__probe5.tsx`、`__probe6.tsx`、`scripts/ssr-decision-feed.tsx`

---
## 轮次 3 · 2026-09-16 21:30（第 40 分钟）

### ✅ 已修复：熔断生效时不再付费请求模型

**改动**：`autoTrader.ts` —— 熔断生效**且本地与交易所两边都没有持仓**时，
在抓行情与请求模型**之前**早退。

**为什么安全**：熔断**只拦新开仓、不拦平仓**。有仓位时模型必须继续跑
（决策里可能有平仓），所以条件必须是「熔断 **且** 空仓」。
两边持仓都为空才跳过——不一致时保守地照常跑。

**实测效果**：

| 周期 | 提示词 | 延迟 | 说明 |
| --- | --- | --- | --- |
| #66（改前） | 58,331 | 67,881ms | 完整调用 |
| **#70 / #71（改后）** | **—** | **0ms** | 跳过 |

**每轮省下约 5.8 万输入 + 9 千输出 tokens。** 熔断期间约 134 万 tokens/小时归零。

### ⚠️ 我在实现过程中自己犯了一个语义错误，已修正

第一版把说明写进了 `progress.error`，于是跳过的周期被记成 **`success=0`（失败）**。

**熔断拦住开仓不是失败，是系统在正常工作。** 而该字段的契约明确写着
「非空即代表本轮失败」（`success: progress.error === null`）。往失败字段里塞
正常状态会导致：成功率统计虚低、**监控把正常状态报成故障**，然后有人去追一个
不存在的问题。

**修正**：说明改放进 `executionLog`，状态用 `skipped`（与熔断拒绝决策时同一个取值）。
现在 `success=1` 且理由照常显示。已验证：#70 / #71 都是 `success=1`。

### 新增的两个测试

1. **`熔断生效且空仓时跳过模型请求，未熔断时照常请求`** —— 同时钉住两件事：
   跳过确实发生（`calls === 0`），以及**未熔断时照常调用**（防止"跳过"变成"再也不决策"）。
   这个测试本身就是变异检查：没有跳过逻辑时 `calls` 必然是 1。
2. **加强既有的回撤熔断测试** —— 从"断言理由文字出现"改为
   **"断言没有任何成功开仓"**。只看理由是不够的：理由写对了但订单照样出去，
   才是真正危险的情况。

### 顺带解开的一个疑问

`当前委托 0` 却列出 16 行——原因是 TraderPage 数的是 WebSocket 事件里的订单
（加载时为空、从不清除），而表格读 REST。另一个代理已让两者共用同一个数组。
**这类"两个地方各算一遍"的问题和之前的字号、内边距是同一类根因。**

---
## 轮次 4 · 2026-09-16 22:05（第 75 分钟）

### ✅ 已修复：日志双写（同一句话出现两遍）

**根因**：`AutoTrader.emit()` 与全局 log sink **各写一次库、各推一次事件**。

```
① log[level]('[测试2] …')  →  全局 sink  →  写库(trader_id=NULL) + 推事件
② runtimeLogs.write(5, …)  →  又写一条
③ eventBus.publish(…)      →  又推一次
```

**修法不是删掉一条**（那会丢机器人归属），而是**让 sink 感知归属**：
`emit()` 把 `{ traderId, raw }` 通过 `meta` 交给 sink，由 sink 统一落盘与推送。
于是 `trader_id` 保住、记录只剩一条、事件只推一次。

实测：机器人日志 35 条，**同毫秒重复 0 处**。

### ✅ 已修复：状态型警告每周期都写

熔断与候选池裁剪**每个周期都会再次成立**，却按事件每轮写一次 —— 一小时 21 条
同一句话，真正的异常被埋掉。**一个每轮都响的警告等于没有警告。**

新增 `emitOnChange(key, level, message)`：状态没变就不再记；**解除时记一条"已解除"**
再清状态，这样"什么时候开始的、什么时候结束的"两个时刻都有记录。
候选池裁剪同理（按文案去重，数字变了会重新记）。

实测（4 个周期的窗口）：

| 日志 | 修复前 | 修复后 |
| --- | --- | --- |
| 单日亏损熔断 | 每轮 2 条 | **只记 1 条（进入时）** |
| 候选池裁剪 | 每轮 1 条 | **0 条** |

### ⚠️ 我在测量时犯了一个错

第一次统计日志速率时用了 `created_at >= datetime('now','-8 minutes')`，
但库里存的是 **ISO 格式**（`2026-09-16T13:52:28.301Z`，带 T 和 Z），
而 `datetime()` 产生的是**空格分隔**格式 —— **字符串比较全部为真**，
于是我读到"8 分钟新增 769 条"这种明显不可能的数字。

改用 `new Date(...).toISOString()` 比对后得到正确结果。
**教训：时间戳跨格式比较前先确认两边的格式一致**，否则会得出任意结论。

### 新的头号噪音源

```
× 13  loaded 528 tradable USDT-M perpetual contracts   ← 10 分钟内 13 次
×  4  [测试2] 对账完成：补录 0 笔，修正 12 笔
×  2  connected to 币安 USDT 本位合约（clock offset 19ms）
```

交易所元数据的 bootstrap 消息**每次重连都写一条**，10 分钟 13 次，
而同期只有 3 个决策周期。**下一轮处理。**

---
## 待人工决定（累积）

1. **生产库 3 行重复成交是否删除**（建议删 #17/#11/#9，保留 #4）
2. **提示词预算 / 候选数是否调整**（当前 60,000 预算打满，11 候选，48 秒延迟）
3. **两份方案待批**：调整止盈止损、让模型看见绩效

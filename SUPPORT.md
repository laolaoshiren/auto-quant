# 获取帮助

先说结论，好让你少走弯路：

| 你想…… | 去哪里 |
| --- | --- |
| 快速跑起来 | [README 的一条命令部署](README.md#-一条命令部署) |
| 理解某个功能怎么用 | 先读 [README](README.md)，再读 [docs/](docs/) |
| 改代码、加功能 | [docs/DEVELOPMENT.md](docs/DEVELOPMENT.md) + [docs/MODULES.md](docs/MODULES.md) |
| 问「为什么我的机器人不下单」 | **先跑策略体检**（见下），再问 [Discussions](/discussions) |
| 报一个可复现的缺陷 | [开 issue](/issues/new/choose)，用 Bug 模板 |
| 提功能建议 | [Discussions](/discussions) 或 Feature 模板 |
| **报告安全漏洞** | **不要开公开 issue** —— 见 [SECURITY.md](SECURITY.md) |
| 遇到真实资金相关的事故 | 用「交易事故」issue 模板，它要求的证据是诊断必需的 |

---

## 遇到问题的第一步：策略体检

「机器人不下单」「模型没反应」「策略好像没生效」—— 这类问题**九成能在控制台的
「策略体检」里直接看到原因**。它对模拟账户跑完整链路，不下任何真实订单，
七个阶段逐个告诉你卡在哪里：

```
交易所连接 → 选出候选标的 → 组装行情与指标 → 构建提示词
→ 调用模型 → 解析模型输出 → 硬风控审查
```

它会明确区分几种容易被混淆的情况：

- **模型没回答**（输出被截断 / 接口返回空）—— 是模型或配置的问题
- **风控拒绝了全部提案** —— 是策略参数的问题，体检会给出拒绝原因
- **选不出候选标的** —— 是选币来源或门槛的问题

先跑它，再提问。把体检结果贴进 issue 或讨论里，能省掉好几轮来回。

---

## 提问前请先做这三件事

1. **搜索**已有的 issue 和 Discussions —— 大概率有人问过
2. **读文档** —— 上面表格里列了每份文档负责回答什么
3. **跑策略体检或 `npm run verify`** —— 它们能在 30 秒内给出比描述更准确的信息

这三点不是礼节性的要求。这个项目的维护者数量很少，而"先自己查一遍"
是让有限的时间花在真正的新问题上的唯一办法。

---

## 提问时请附上

缺了这些信息，问题往往无法诊断，只能来回追问：

- **你在哪一档跑的**：本地源码 / Docker / 模拟盘 / 实盘
- **环境**：`/api/health` 的返回（它会告诉你环境、版本、数据库路径）
- **策略体检的结果**（如果是"不交易"类问题）
- **相关日志**：`docker logs autoquant` 或 `journalctl -u autoquant`
- **可复现的步骤**：你点了什么、期待什么、实际发生了什么

> ⚠️ **贴日志或截图前请先脱敏。** 日志里可能包含账户余额、持仓、标的；
> 环境中可能包含服务器地址。**API Key 与密码绝不能贴出来。**
> 如果不确定某段是否敏感，先删掉再说 —— 我们宁可多问一句。

---

## 我们不能帮你做的事

说清楚边界，比含糊地承诺"尽力而为"更有用：

- **不提供投资建议，不评价你的策略能不能赚钱。** 这个项目提供执行框架与风控约束，
  不提供收益判断。问"这个策略能不能盈利"我们答不了
- **不帮你承担交易损失。** 见 [LICENSE](LICENSE) 与 [README 的风险提示](README.md#-风险提示)
- **不协助绕过交易所的地理限制或条款。** 交易所会在 API 层面强制资格限制，
  这个限制是有意为之的。遇到 `restricted location` 类错误，请向交易所确认你的资格
- **不代管你的密钥。** 任何时候都不要把 API Key 发给任何人，包括维护者

---

## 响应时间

这是一个**由贡献者利用业余时间维护**的项目。没有服务水平承诺：

- 安全漏洞会**优先**处理
- 涉及资金安全的缺陷会优先于功能请求
- 其他 issue 和讨论按维护者的时间安排

如果你需要更快的响应，**最有效的办法是提一个 PR** —— 哪怕只是一个能复现问题的测试。
一个带着复现用例的 bug 报告，合并速度通常远快于一个只有描述的。

# 路线图

本文档说明项目**往哪里走**，以及**为什么是这个顺序**。

> 路线图表达的是方向，不是承诺。优先级会随实际情况调整 —— 尤其是当某个改动
> 会影响资金安全时，它会被插到最前面。

---

## 当前状态

**v0.1.0 —— 核心链路可用。**

「自然语言策略 → 硬风控裁决 → 执行 → 完整审计」这条链路已经打通并经过实盘验证：
真实下单、交易所侧止损止盈、触发后正确记账、账目与交易所逐位对账。
部署已经容器化，一条命令即可起服务。

---

## 排序原则

路线图的顺序不是按"功能看起来多酷"，而是按这个顺序：

1. **先保证不出错** —— 任何可能让仓位失去保护、或让账目失真的问题优先修
2. **再保证能验证** —— 一个功能如果没法在不花钱的前提下验证，加进来就是负担
3. **然后扩展覆盖面** —— 更多交易所、更多标的、更多交易形式
4. **最后才是体验** —— 界面、易用性、性能

理由很直接：这是会亏钱的系统。一个漂亮但风控有漏洞的版本，比一个朴素但可靠的版本危险得多。

---

## 近期

### 可靠性

- [ ] **资金费的实盘验证** —— 归集逻辑已实现并有测试覆盖，但还没有在真实的
      8 小时结算上验证过（需要持仓跨过结算点）。这是目前唯一未被实盘检验的口径
- [ ] **长时间运行的稳定性** —— 连续运行数周的观察：内存、WebSocket 重连、
      交易所侧订单状态漂移
- [ ] **并发场景** —— 多个机器人共用一个账户时的边界情况

### 可验证性

- [ ] **扩展模拟回放** —— 覆盖更多市场形态（急跌、横盘、跳空），
      以及资金费结算的模拟
- [ ] **策略体检的扩展** —— 目前是单轮检查，可以做多轮并给出稳定性评估

### 扩展

- [ ] **新增交易所适配器** —— 适配层的抽象已经就位（连接、合约元数据、下单、
      行情统一接口），新增交易所主要是实现适配层 + 处理该交易所特有的契约
- [ ] **现货与更多标的类型** —— 当前是合约；适配层设计时没有假设只有合约
- [ ] **更多交易形式** —— 限价挂单、分批建仓、移动止损

---

## 待办：大版本依赖升级

Dependabot 提出了四项大版本升级，评估后**暂缓**——每一项都需要独立一轮迁移与全量回归，
而不是直接合入。它们不紧急（当前版本都受支持），但会在某个时点变成必须做的事。

| 升级 | 影响面 | 为什么需要单独一轮 |
| --- | --- | --- |
| **React 18 → 19** | `packages/web` 全部组件 | refs 与并发渲染有行为差异 |
| **Tailwind 3 → 4** | `tailwind.config.js` + 设计令牌 | 配置格式从 JS 改为 CSS-first，`packages/web/DESIGN.md` 描述的整套令牌要重写 |
| **TypeScript 5.7 → 7.0** | 整个 monorepo | 编译器更换实现，需先确认诊断行为无差异 |
| **zod 3 → 4** | `StrategyConfigSchema` 等 | 校验 API 有破坏性变更，而 zod 是本项目策略配置的**唯一事实来源** |
| **react-router-dom 6 → 7** | 全部路由 | 路由 API 破坏性变更 |

**升级一项时的验收标准**（缺一不可）：

```
npm run typecheck && npm test && npm run sim
npm run build && npm run ui:smoke     # 前端相关
```

`ui:smoke` 是必须的：typecheck 与 build 通过**不代表页面能用**，
这个项目里已经有过 `animate-pulseSoft` 这种"编译全绿但功能是死的"实例。

React 19 与 Tailwind 4 建议**一起做**——两者都触及 `packages/web` 的渲染层，
分两次回归是重复劳动。

---
## 中期

- [ ] **策略回测** —— 用历史数据评估一个策略，而不只是验证它能跑通
- [ ] **多账户支持** —— 一个实例管理多个交易所账户
- [ ] **通知渠道** —— 关键事件（开平仓、熔断、异常）推送到外部
- [x] **控制台代码分割** —— 已完成：路由级 `lazy()`，`recharts` / `lightweight-charts` 各自独立
      chunk，首屏只加载外壳与当前页面

---

## 长期 / 待讨论

这些方向还没有明确的设计，列出来是为了让讨论有起点：

- **策略市场 / 共享** —— 允许用户互相分享策略配置（提示词 + 风控参数）
- **参数寻优** —— 在给定策略框架下自动搜索风控参数
- **组合层面的风控** —— 跨机器人、跨标的的总敞口管理

> 需要先想清楚的问题：这些功能会不会让项目从"工具"变成"建议"？
> 后者在很多辖区有监管含义。见 [docs/LEGAL.md](docs/LEGAL.md)。

---

## 不做的事

明确划出边界，比列出想做的事更重要：

- ❌ **不提供任何绕过交易所地理限制的功能。** 交易所会在 API 层面强制资格限制，
  这个限制是有意为之的，绕过它既违反条款也有法律风险
- ❌ **不承诺盈利。** 这个项目提供的是执行框架与风控约束，不是收益
- ❌ **不替用户管理资金。** 不做代客交易、不做托管 —— 那会把项目变成受监管活动
- ❌ **不削弱风控来换取更好的回测数据。** 如果某个限制让策略表现变差，
  那是策略的问题，不是限制的问题
- ❌ **不为了兼容而保留已知错误的账目口径。** 账目必须能和交易所对上

---

## 如何参与

- 对某个方向有想法 → 在 [Discussions](/discussions) 里开一个话题
- 想直接动手 → 先看 [`good first issue`](/labels/good%20first%20issue) 标签的议题
- 想提一个较大的改动 → 先开 RFC issue，见 [GOVERNANCE.md](GOVERNANCE.md#需要-rfc-的变更)

**当前的瓶颈不是想法，是经过验证的实现。** 最有价值的贡献往往是：
把一个"应该没问题"的地方，变成"我验证过了，这是证据"。

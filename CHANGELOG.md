# 变更日志

本文件记录本项目的所有重要变更。

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

> **本项目处于 0.x 阶段。** 按语义化版本的定义，0.x 的次版本号可以包含破坏性变更。
> 但我们仍然会把破坏性变更**单独列在「破坏性变更」小节**，而不是混在「新功能」里 ——
> 一个交易系统的配置或数据格式变更会直接影响正在运行的实例，把它们埋起来是不负责任的。
>
> 从数据库结构到策略配置的变更，都会在对应版本下注明**是否需要迁移动作**。

---

## [Unreleased]

### 计划中

见 [ROADMAP.md](ROADMAP.md)。

---

## [0.1.0] - 2026-09-16

首个可用版本。核心是「自然语言策略 + 硬风控 + 完整决策审计」这条链路，
以及一条命令即可部署的容器化运行方式。

### 新增

**交易核心**

- 自然语言策略：选币、指标、风控、提示词四组配置，控制台可视化编辑，
  内置稳健 / 进取 / 短线三套预设
- 硬风控引擎：杠杆上限、仓位上限、名义价值预算、强制止损止盈、盈亏比门槛、
  回撤保护、熔断与节流。**模型只能提议，无法绕过**
- 完整决策审计：每一轮保存完整提示词、模型原始响应、思维链、
  以及风控的每一次钳制与拒绝及其原因
- 交易所适配层：连接、合约元数据、签名请求、下单、条件单、行情与用户数据流。
  当前实现的适配器为币安合约
- 持仓对账与成交历史重建：从交易所的成交记录补齐进程未运行时发生的平仓
- 盈亏口径与交易所对账：净 = 毛 − 手续费 − 资金费；手续费记双侧，资金费从收入流水归集

**模型接入**

- 多提供商支持：OpenAI 兼容端点、Anthropic、Gemini
- 模型列表可从厂商接口**实时拉取**，而不是维护一份会过期的静态清单
- 错误分类与重试策略，含对「临时不可用」类错误的识别

**控制台**

- 机器人看板：统计卡、净值曲线、持仓 / 委托 / 成交 / 订单四个视图、
  实时决策流（决策常显，思考过程按需展开）
- 策略工作室：完整字段编辑 + **策略体检**（不接交易所、不下单即可验证整条链路）
- 交易所与模型管理，含连接测试与**实时余额展示**
- 事件流采用 WebSocket 推送

**部署**

- Docker 多阶段构建，服务以镜像运行，服务器无需 Node 或源码
- 一条命令部署：`cd deploy && ./up.sh`，自动生成账号与密钥、拉取镜像、
  启动并等待健康检查
- GitHub Actions 在 CI 通过后自动构建并推送镜像到 GHCR
- 数据备份与迁移（`./up.sh backup` / `import`），含主密钥自动对齐

**验证工具**

- `npm run verify` —— 用真实行情跑通「选币 → 指标 → 提示词 → 解析 → 风控」
- `npm run sim` —— 历史 K 线回放 + 会真正触发止损止盈的模拟交易所，15 项校验
- `npm run sim:live` —— 同上，但使用真实模型
- 实盘冒烟测试脚本：最小仓位走完开仓 → 挂止损 → 挂止盈 → 回读确认 → 撤单 → 平仓

### 安全

- 交易所凭据以 AES-256-GCM 加密存储，主密钥与数据库分离
- 密码使用 scrypt 哈希
- 无注册入口：管理员账号在首次启动时自动创建，用户名与密码均随机生成
- 修改凭据必须验证当前密码
- 提交前钩子拦截密钥与站点专属信息；CI 另有独立的仓库卫生检查
- 第三方依赖许可证合规自动化：遇到 copyleft 依赖直接失败

### 文档

- `docs/DEVELOPMENT.md` 开发手册 · `docs/MODULES.md` 模块地图与扩展配方 ·
  `docs/AGENTS.md` AI 代理指南 · `docs/API.md` 接口参考 ·
  `docs/DEPLOYMENT.md` 部署指南 · `docs/ARCHITECTURE.md` 设计取舍 ·
  `docs/LEGAL.md` 合规清单 · `docs/research/` 一手调研记录

### 已知限制

- 交易所适配层目前只实现了币安合约；其他交易所可按下述配方扩展
- 条件单与保护单的实现依赖币安的 Algo Order 接口
- 控制台尚未做代码分割，首屏包含单个约 1 MB 的 JS 包
- 资金费归集依赖交易所的收入流水接口，读取失败时该项记 0 并在日志中说明

---

[Unreleased]: https://github.com/laolaoshiren/auto-quant/compare/v0.1.0...HEAD
[0.1.0]: https://github.com/laolaoshiren/auto-quant/releases/tag/v0.1.0

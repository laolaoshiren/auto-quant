# Docker 构建上下文排除项
#
# 目的有两个：构建更快，以及**绝不让密钥进入镜像层**。
# 镜像层是不可变的：一旦某个文件被 COPY 进去，即使后续层删掉它，
# 它仍然留在镜像历史里，任何拿到镜像的人都能取出来。

# --- 依赖与构建产物（镜像里自己装、自己构建） --------------------------------
node_modules
**/node_modules
**/dist
*.tsbuildinfo

# --- 运行时数据：绝不进镜像 ---------------------------------------------------
# data/ 里有 SQLite 数据库（加密凭据）、AES 主密钥、JWT 密钥、日志。
# 主密钥与数据库放在一起 = 明文凭据。
data
**/data
*.sqlite
*.sqlite-wal
*.sqlite-shm
*.master.key

# --- 密钥与环境配置 -----------------------------------------------------------
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
deploy.local.ps1
sensitive-patterns.txt

# --- 版本控制与 CI（镜像里不需要） --------------------------------------------
.git
.gitignore
.github

# --- 部署编排文件（镜像里不需要，它是给宿主机用的） ---------------------------
deploy

# --- 文档与调研资料（体积大，运行时用不到） -----------------------------------
# 注意：docs/ 里没有任何运行时代码依赖，排除它是安全的。
docs
*.md
!README.md

# --- 其它 ---------------------------------------------------------------------
_deploy.tar.gz
*.tar.gz
*.log
.vscode
.idea

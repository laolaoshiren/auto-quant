# =============================================================================
# 换行符规范化
#
# -----------------------------------------------------------------------------
# 为什么必须有这个文件
# -----------------------------------------------------------------------------
# 这个项目**在 Windows 上开发、部署到 Linux**。没有这层约束时，编辑器/脚本
# 会把换行写成 CRLF，而 Linux 上的 bash 会把行尾的 \r 当成命令的一部分：
#
#     $'\r': command not found
#     syntax error: unexpected end of file
#
# 这不是假设 —— `scripts/deploy.ps1` 曾经被写进 CRLF，于是**部署直接失败**，
# 报的还是一条完全指不到"换行符"的错误。任何在 Windows 上编辑过 shell 脚本
# 的人都遇到过这个坑，而它每次都要花时间才能认出来。
#
# 所以：**一律 LF**，包括 .ps1。PowerShell 完全能读 LF 的脚本；
# 但反过来（Linux 读 CRLF）会坏，这个方向不可逆。
# =============================================================================

# 默认：仓库里一律 LF，检出时也一律 LF
* text=auto eol=lf

# 明确指定为文本的文件类型
*.ts    text eol=lf
*.tsx   text eol=lf
*.js    text eol=lf
*.mjs   text eol=lf
*.jsx   text eol=lf
*.json  text eol=lf
*.md    text eol=lf
*.yml   text eol=lf
*.yaml  text eol=lf
*.css   text eol=lf
*.html  text eol=lf
*.sh    text eol=lf

# .ps1 也用 LF。
#
# 直觉上 Windows 脚本该用 CRLF，但这里不行：`scripts/deploy.ps1` 内嵌了一段
# bash（here-string），它会被原样送到 Linux 上执行。脚本文件的换行符就是那段
# bash 的换行符 —— .ps1 用 CRLF，等于给远端送一份跑不起来的脚本。
# PowerShell 读 LF 没有任何问题，所以这个方向是安全的。
*.ps1   text eol=lf

# 这些不该被改动换行
*.png   binary
*.jpg   binary
*.jpeg  binary
*.gif   binary
*.webp  binary
*.ico   binary
*.svg   text eol=lf
*.woff  binary
*.woff2 binary
*.sqlite binary
*.sqlite3 binary

# 锁文件由 npm 生成，不要动它
package-lock.json -diff linguist-generated
